# Virtual environment — never commit, it's huge and machine-specific
venv/

# The vector database — rebuildable anytime with `python ingest.py`

# Python cache files
__pycache__/
*.pyc

# Mac system files
.DS_Store

# Environment files (in case you ever store keys in one)
.env
